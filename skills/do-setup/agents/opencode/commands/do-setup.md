---
description: Inicializa o projeto, identifica skills relevantes e atualiza o arquivo de configuração do projeto com resumo, convenções e skills disponíveis. Aceita o argumento "agents" para instalar apenas os agentes de orquestração sem rodar o setup completo.
---

# /do-setup

Use a skill `do-setup` e execute o procedimento completo descrito nele na íntegra.

## Argumento opcional

- Sem argumento: executa o setup completo (análise do projeto + instalação de agentes).
- `agents`: instala apenas os agentes e comandos de orquestração, sem rodar o setup completo.

## Exemplo

```
/do-setup
/do-setup agents
```
